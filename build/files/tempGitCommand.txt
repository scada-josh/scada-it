git commit -m "2015-09-15 v2 scada-it(initial addNewRtuInfoManager)"

git commit -m "Added CTU-800 Merge Master Already"

git checkout -b addNewRtuInfoManager
STEP#01 : ค้นหาด้วยชื่อ DM และกำหนดให้ทุก DM ที่เจอมี Status = 0
STEP#02 : ค้นหาด้วย ip_dm-xx-xx-xx-xx ที่เป็น Unique ถ้าเจอให้อัพเดต status=1 และกำหนดให้คอลัมภ์ comments เป็น #B54 B&R  X20 GPRS

